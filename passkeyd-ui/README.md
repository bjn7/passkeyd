# Passkeyd UIs

This includes the user interfaces for passkeyd:

- **`passkeyd-ice`**: Universal custom UI that runs everywhere.
- **`passkeyd-kde`**: Native UI built specifically for KDE Plasma.
- **`passkeyd-gtk`**: Native UI built specifically for GTK-based environments (GNOME, XFCE, etc.).
