name: Passkeyd Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install target
        run: rustup target add x86_64-unknown-linux-gnu

      - name: Install sys dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y clang llvm-dev libudev-dev libclang-dev libpam0g-dev libtss2-dev gh libglib2.0-dev pkg-config cmake libgtk-4-dev qt6-base-dev qt6-declarative-dev lld

      - name: Compile
        run: cargo build --release --target x86_64-unknown-linux-gnu

      - name: Compress release
        run: |
          tar czf passkeyd-x86_64-unknown-linux-gnu.tar.gz \
            -C target/x86_64-unknown-linux-gnu/release passkeyd passkeyd-manager passkeyd-migrate \
            passkeyd-ice-enroll passkeyd-ice-select passkeyd-ice-selection \
            passkeyd-kde-enroll passkeyd-kde-select passkeyd-kde-selection \
            passkeyd-gtk-enroll passkeyd-gtk-select passkeyd-gtk-selection \
            -C $GITHUB_WORKSPACE passkeyd.service passkeyd.conf icons theme.conf
      - name: Create GitHub Release
        run: |
          gh release create "${{ github.ref_name }}" passkeyd-x86_64-unknown-linux-gnu.tar.gz \
              --title "Release ${{ github.ref_name }}" \
              --notes "Automated build for version ${{ github.ref_name }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
